const { Attendee } = require("./attendees.js");

let findByLastName = () => {
  // Return all people have have a specific last name
};

let updateAttendee = () => {
  // Update the specific attendee selected
}

module.exports = {findByLastName, updateAttendee}